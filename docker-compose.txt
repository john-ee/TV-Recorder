services:
  tv-recorder:
    build: .
    container_name: tv-recorder-test
    restart: unless-stopped
    ports:
      - "5010:5000"
    volumes:
      # Mount channels config (can be edited on host)
      - ./channels.json:/app/channels.json:ro
      # Mount recordings directory
      - /dump/conversions/disks:/recordings
      # EPG and schedules cache (for persistence across restarts)
      - ./data:/tmp
    environment:
      - TZ=Europe/Paris
      - PYTHONUNBUFFERED=1
      - DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1434115229511581759/3lwKvReE9mCiRI8qFZn12rtN6_zxsRoMZTTzF5RY4O9flyAyBYuFpg1EMS5gKbtska-1
    networks:
      - tv-recorder-net
    
networks:
  tv-recorder-net:
    driver: bridge
